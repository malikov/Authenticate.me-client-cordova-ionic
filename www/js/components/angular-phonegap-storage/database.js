'use strict';

/*
  angular component for using PhoneGap's database
*/

angular.module('malikov.phonegap.storage',
  ['btford.phonegap.ready']).
  factory('database', function (phonegapReady) {
    /*
      will add stuff later :)
    */
    var database = {
      
    };

    return database; 

  });